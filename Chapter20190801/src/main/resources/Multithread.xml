<?xml version="1.0" encoding="UTF-8" ?>

<suite name="thread" parallel="methods" thread-count="2">
    <!--
    tests级别:不同的test tag下的用例可以在不同的线程下执行
     相同的 test tag 只能在相同的线程下执行
     class 级别: 相同的 class tag 下的用例在同一个线程中执行  不同的class tag 下的用例可以在不同的线程中执行
    methods级别: 所有用例都可以在不同的线程下去执行
    thread-count:代表了最大并发线程数
    -->
    <test name="demo1">
        <classes>
            <class name="com.gplbj.testng.multThread.MultithreadOnXml"></class>
        </classes>
    </test>

</suite>